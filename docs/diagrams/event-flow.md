# Event flow — generated

> **Generated by `make diagrams` (`tools/contracts.py`) — do not edit by hand.**
> The producer edges come from `EVENT_TOPICS` and the component that publishes each
> event; the consumer edges come from the saga, projection and write-side consumer
> registries. A test fails if this file and the code disagree.
>
> A consumer edge is drawn only when it crosses a bounded context: an event's own
> context consuming it is the default, and drawing every one of those would turn
> the diagram into a restatement of `docs/events.md`.

```mermaid
graph LR
  subgraph produce [Producers]
    relay -->|"CareMissed, CareScheduleCreated, CareSkipped, WateringCompleted, WateringDue, WateringRescheduled"| careevents
    relay -->|"SpeciesAdded, SpeciesCacheInvalidated, SpeciesSyncRequested, SpeciesUpdated"| catalogevents
    relay -->|"PlantAdded, PlantMoved, PlantOnboarded, PlantRemoved"| gardenevents
    relay -->|"JournalEntryAdded"| journalevents
    relay -->|"NotificationCreated, NotificationRead"| notificationsevents
    relay -->|"SensorOffline, SoilMoistureHigh, SoilMoistureLow, TemperatureAnomaly"| telemetryevents
    sagas -->|"SagaCompensated, SagaCompleted, SagaFailed, SagaStarted"| sagaevents
    ingress -->|"TelemetryReceived"| telemetryevents
    careevents["care.events"]
    catalogevents["catalog.events"]
    gardenevents["garden.events"]
    journalevents["journal.events"]
    notificationsevents["notifications.events"]
    sagaevents["saga.events"]
    telemetryevents["telemetry.events"]
  end
  subgraph consume [Cross-context consumers]
    Care -->|"CareMissed"| Notifications
    Telemetry -->|"SoilMoistureHigh"| Care
    Telemetry -->|"SoilMoistureLow"| Notifications
    Telemetry -->|"TelemetryReceived"| Care
    Telemetry -->|"TemperatureAnomaly"| Notifications
    Care -->|"WateringCompleted"| Journal
    Care -->|"WateringDue"| Notifications
    Care -->|"WateringRescheduled"| Notifications
  end
  note["no cross-context consumer: CareScheduleCreated, CareSkipped, JournalEntryAdded, NotificationRead, PlantMoved, PlantOnboarded, PlantRemoved, SagaCompensated, SagaCompleted, SagaFailed, SagaStarted, SensorOffline, SpeciesAdded"]
```
