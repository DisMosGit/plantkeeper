"""Read models of the ``read_analytics`` schema.

They are Django's, not SQLAlchemy's: the read side is a Django application, and a
second table definition for the same table would only be a thing to keep in sync.
"""
