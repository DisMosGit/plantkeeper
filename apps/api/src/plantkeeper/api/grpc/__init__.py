"""The gRPC write API: the same application layer as the REST process.

The generated protobuf stubs live under ``generated/`` and are not committed —
``make proto`` produces them from ``proto/``. See ``docs/grpc.md``.
"""
